# Capture overlay (planned)

Home of the region-selection overlay: a borderless fullscreen window that shows the
frozen capture, lets the user drag a rectangle, and crops the bitmap in TypeScript.

See "Known gap (MVP)" in docs/ARCHITECTURE.md. Route the design through the
`architect` agent before implementing.
